{% extends "layout/base.twig" %}

{% block title %}Pobierz{% endblock %}

{% block content %}
<section class="container-fluid text-center section page-header">  
	<h2>{{ template.namePl }} - {{ cms.namePl }}</h2>
	<h3>{{ template.description }}</h3>
</section>		
		
<div class="container-fluid bg-gray section info">
	<article class="container">
		<p>
			Proszę uzupełnij nazwę instytucji i adres email w poniższym formularzu.
			Na wskazany adres wyślemy wiadomość, w której znajdziesz link do pobrania paczki instalacyjnej z szablonem oraz instrukcję,
			która pomoże Ci w instalacji szablonu.
		</p>
		<p>
		    Gdybyś potrzebował pomocy skontaktuj się z nami przez <a href="/kontakt">formularz kontaktowy</a>.
		</p>
    </article>

	<div class="container-fluid bg-gray section ">
		<div class="container clearfix">
			<div class="col-sm-8 col-sm-offset-2 ">
				<form role="form" action="/pobierz/{{ template }}/{{ cms }}" method="post">
					<div class="form-group">
						<input title="Nazwa instytucji" type="text" class="form-control" id="name" name="name" maxLength="50"
							placeholder="Nazwa instytucji (wymagane)"
							required>
					</div>
					<div class="form-group">
						<input title="email" type="email" class="form-control" id="administrativeEmail" name="administrativeEmail" maxLength="50"
							   placeholder="Email (wymagane)"
							   required>
					</div>
					<button title="Wyślij" type="submit" id="submit" name="submit" class="btn pull-right cta cta-1">Wyślij</button>
			    </form>
        	</div>
        </div>
    </diiv>
</div>

{% endblock %}