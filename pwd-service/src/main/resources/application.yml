spring:
  jpa:
    database-platform: org.pwd.hibernate.JsonPostgreSQLDialect
    hibernate:
      ddl-auto: update
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/pwd
    username: pwd
    password: pwd
    initialize: false
    continue-on-error: false
  http:
    encoding:
      charset: UTF-8

analysis:
  endpoint: https://blooming-lowlands-9234.herokuapp.com/analysis

mailgun:
  mailbox: kontakt@pwd.dolinagubra.pl
  apikey: dummyApiKey
  domain: localhost

error:
  whitelabel:
    enabled: false